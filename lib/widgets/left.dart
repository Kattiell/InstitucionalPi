import 'package:flutter/widgets.dart';

class Left extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Apenas testando CONTATO"),
        Text("Apenas testando CONTATO"),
        Text("Apenas testando CONTATO"),
        Text("Apenas testando CONTATO"),
        Text("Apenas testando CONTATO"),
        Text("Apenas testando CONTATO"),
        Text("Apenas testando CONTATO"),
        Text("Apenas testando CONTATO"),
      ],
    );
  }
}
