import 'package:flutter/widgets.dart';

class Right extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Apenas testando LOCALIZAÇÃO"),
        Text("Apenas testando LOCALIZAÇÃO"),
        Text("Apenas testando LOCALIZAÇÃO"),
        Text("Apenas testando LOCALIZAÇÃO"),
        Text("Apenas testando LOCALIZAÇÃO"),
        Text("Apenas testando LOCALIZAÇÃO"),
        Text("Apenas testando LOCALIZAÇÃO"),
        Text("Apenas testando LOCALIZAÇÃO"),
      ],
    );
  }
}
